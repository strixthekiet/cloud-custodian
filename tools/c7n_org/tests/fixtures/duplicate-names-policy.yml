policies:
  - name: duplicate-name
    resource: aws.ec2
    filters:
      - tag:Name: present

  - name: duplicate-name
    resource: aws.s3
    filters:
      - type: bucket-encryption
        state: false
